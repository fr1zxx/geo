#!/bin/bash
# GEO Plugin CLI wrapper script
# This script provides the /plugin command interface

# Get the directory where this script is located
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Run the Python plugin manager
python3 "$SCRIPT_DIR/plugin.py" "$@"
